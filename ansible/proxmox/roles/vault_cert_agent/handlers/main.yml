---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart vault cert agent
  ansible.builtin.systemd_service:
    name: "{{ vault_cert_agent_unit_name }}"
    state: restarted
