---
- import_playbook: powerdns_servers.yml
- import_playbook: vault_server.yml
- import_playbook: ../../ansible/proxmox/provision_certificates.yml
  tags:
    - certs
    - proxmox_certs
- import_playbook: ../../ansible/proxmox/ceph_admin_portal.yml
  tags:
    - never
    - ceph_dashboard
    - ceph_admin_portal
- import_playbook: ../../ansible/proxmox/ceph_admin_gateway.yml
  tags:
    - never
    - ceph_admin_gateway
# Audit-only by default; pass -e ceph_frag_compact=true to remediate.
- import_playbook: ../../ansible/proxmox/ceph_bluestore_fragmentation.yml
  tags:
    - never
    - ceph_fragmentation
- import_playbook: ../../ansible/proxmox/ceph_fragmentation_metrics.yml
  tags:
    - never
    - ceph_fragmentation_metrics
    - metrics
