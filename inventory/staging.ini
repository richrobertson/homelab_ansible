[dbservers]
postgresql[01:01].stage.myrobertson.net

[powerdns]
192.168.7.201

[powerdns-recurse]
192.168.7.202

[vault]
# NOTE: This compatibility/staging inventory currently defines no Vault hosts.
# Vault provisioning and Proxmox cert automation targeting `hosts: vault` will
# be skipped when using this file. Add the appropriate staging Vault host(s)
# here if this inventory is to be used for real provisioning.
# Staging Vault hosts (add hostnames/IPs here as needed)